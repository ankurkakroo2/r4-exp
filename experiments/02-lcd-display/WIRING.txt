╔══════════════════════════════════════════════════════════════════╗
║                   LCD I2C WIRING DIAGRAM                         ║
╚══════════════════════════════════════════════════════════════════╝

COMPONENTS NEEDED:
  • Arduino Uno R4 Minima
  • 16x2 LCD Display with I2C Backpack (small blue/green PCB on back)
  • 4 Jumper Wires (Male-to-Female or Male-to-Male)

───────────────────────────────────────────────────────────────────

CONNECTION TABLE:

  LCD I2C Pin  →  Arduino Pin   Wire Color (Suggested)
  ───────────────────────────────────────────────────
  GND          →  GND           Black/Brown
  VCC          →  5V            Red
  SDA          →  A4 (or SDA)   Yellow/Green
  SCL          →  A5 (or SCL)   Blue/White

───────────────────────────────────────────────────────────────────

VISUAL DIAGRAM:

Arduino Uno R4 Minima                    LCD I2C Module
┌─────────────────────┐                 ┌──────────────┐
│                     │                 │  I2C Backpack│
│         POWER       │                 ├──────────────┤
│         ─────       │                 │GND VCC SDA SCL
│    GND  •  • 5V     │                 │ •   •   •   • │
│         │    │      │                 └──┼───┼───┼───┼┘
│         │    │      │                    │   │   │   │
│         │    └──────┼────────────────────┘   │   │   │
│         └───────────┼────────────────────────┘   │   │
│                     │                            │   │
│      ANALOG         │                            │   │
│      ───────        │                            │   │
│  A5  •  SCL ────────┼────────────────────────────┘   │
│  A4  •  SDA ────────┼────────────────────────────────┘
│  A3  •              │
│  A2  •              │                 ┌──────────────┐
│  A1  •              │                 │              │
│  A0  •              │                 │  LCD SCREEN  │
│                     │                 │  16 x 2      │
│                     │                 │  Characters  │
└─────────────────────┘                 └──────────────┘

───────────────────────────────────────────────────────────────────

STEP-BY-STEP WIRING:

1. IDENTIFY THE I2C PINS ON LCD:
   Look at the back of your LCD. You should see 4 pins labeled:
   GND, VCC, SDA, SCL (sometimes in different order)

2. CONNECT POWER (Can't damage anything):
   • LCD GND  →  Arduino GND (any GND pin)
   • LCD VCC  →  Arduino 5V

3. CONNECT I2C COMMUNICATION:
   • LCD SDA  →  Arduino A4 (or dedicated SDA pin if available)
   • LCD SCL  →  Arduino A5 (or dedicated SCL pin if available)

4. DOUBLE-CHECK:
   ✓ GND to GND (black wire)
   ✓ VCC to 5V (red wire)
   ✓ SDA to A4 (data line)
   ✓ SCL to A5 (clock line)

───────────────────────────────────────────────────────────────────

IMPORTANT NOTES:

📌 I2C Address:
   Most LCD I2C modules use address 0x27 or 0x3F.
   If one doesn't work, try the other in the code.

📌 Contrast Adjustment:
   On the I2C backpack, there's a small blue potentiometer (screw).
   If you see nothing on screen:
   • Turn it clockwise slowly
   • You should see the contrast change
   • Adjust until text is clearly visible

📌 Backlight:
   Some I2C modules have a jumper to enable/disable backlight.
   Make sure it's connected if your screen is dark.

───────────────────────────────────────────────────────────────────

TESTING:

Once wired:
1. Connect Arduino to computer via USB
2. Upload the code
3. If nothing appears:
   a) Check all 4 wire connections
   b) Adjust contrast potentiometer
   c) Try different I2C address (0x27 or 0x3F)

───────────────────────────────────────────────────────────────────

COMMON I2C ADDRESSES:
  • 0x27 (Most common for PCF8574 based modules)
  • 0x3F (Alternative address)

To find your LCD's address, run the I2C scanner code (in README.md)

═══════════════════════════════════════════════════════════════════
